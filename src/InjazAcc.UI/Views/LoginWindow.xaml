<Window x:Class="InjazAcc.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:InjazAcc.UI.ViewModels"
        mc:Ignorable="d"
        Title="تسجيل الدخول" Height="360" Width="520">
<Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="نظام محاسبي للحراسات — دخول" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>
        <Border Grid.Row="1" Background="#1FFFFFFF" Padding="20" CornerRadius="12">
            <StackPanel>
                <TextBlock Text="اسم المستخدم" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="كلمة المرور" Margin="0,12,0,0"/>
                <PasswordBox x:Name="Pwd" PasswordChanged="Pwd_PasswordChanged"/>
                <Button Content="دخول" Margin="0,16,0,0" Click="Login_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
