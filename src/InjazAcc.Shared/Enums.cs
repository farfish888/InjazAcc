namespace InjazAcc.Shared;

public enum AccountType { Asset, Liability, Equity, Revenue, Expense }
public enum DocType { Invoice = 1, Receipt = 2, Expense = 3 }
public enum UserRole { Supervisor, Accountant, Reader }
public enum PaymentMethod { Cash, Bank, Cheque }
